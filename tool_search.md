**Claude Code 里的 Tool 选择与搜索是怎么实现的**

如果把这套机制拆开看，你会发现它不是“模型在背后自己检索工具”，而是一个典型的“客户端编排 + 模型决策 + 执行校验”的三段式系统。

它的核心目标很明确：

- 让模型只看到当前真正可用的工具
- 在工具很多时，避免把全部 schema 一次性塞进上下文
- 对某些工具做延迟加载，必要时再通过搜索把它们找出来
- 在模型选中了 tool 之后，再由本地执行层做严格校验和路由

---

## 1. 先理解：模型看到的不是“全部工具”，而是一个经过筛选的工具池

工具系统的第一步，不是让模型选工具，而是先决定“哪些工具有资格被放进本轮请求”。

这个工具池主要分三层：

- 内置工具
- MCP 工具
- 运行时动态附加的工具

### 内置工具怎么来

系统会先定义一组“基础工具全集”，比如文件读写、搜索、终端、计划模式、任务工具、问答工具、Skill 工具等。然后再根据环境变量、功能开关、当前运行模式去决定哪些工具启用。

这里有几个关键过滤点：

- 简单模式下只保留极少数基础工具
- REPL 或协调者模式下会替换成特定的工具组合
- 某些工具只在特定用户类型或功能开关下存在
- 还有一层 deny rules，会把被权限策略禁止的工具从模型视野里移除

也就是说，模型从来不是面对“全宇宙工具”，而是面对“当前会话允许的工具集合”。

### MCP 工具怎么来

MCP 工具是外部服务器提供的工具，它们会被合并进工具池，但同样要经过权限规则过滤。

这一步很重要，因为它决定了模型能否看见某个外部能力。  
如果一个 MCP server 被 deny 掉，那它提供的工具甚至不会出现在 prompt 里，模型也就不可能调用它。

---

## 2. 工具不是只有名字，真正送给模型的是“工具 schema”

模型能不能正确选 tool，关键不在于工具名，而在于工具 schema 是否被正确传入。

每个工具最终都会被转换成 API 能识别的工具定义，通常包括：

- 工具名
- 工具描述
- 输入 schema

其中最关键的是工具描述。  
因为模型选择 tool 的时候，依赖的是它在 prompt 里看到的这些信息，而不是本地代码里的实现细节。

这意味着工具设计本身就非常重要：

- 工具名要有辨识度
- 描述要清晰地说明能力边界
- 输入 schema 要尽量让模型容易生成正确参数

如果 schema 太复杂，模型就更容易输出错误输入。  
所以这套系统会尽量把工具说明做得足够“可被模型理解”。

---

## 3. 为什么要有 ToolSearch：因为不是所有工具都应该一开始暴露给模型

如果工具数量很少，直接全部送进 prompt 没问题。  
但一旦工具变多，尤其是 MCP 工具很多时，问题就来了：

- prompt 变长
- 上下文被工具 schema 挤占
- 工具变更会更频繁地破坏缓存
- 模型在过多工具里更容易选错

于是就引入了 ToolSearch 机制。

它的核心思想是：

- 一些工具不直接给模型完整 schema
- 这些工具先以“可搜索的候选项”形式存在
- 模型如果需要它们，先调用 ToolSearch
- ToolSearch 返回匹配到的工具名
- 下一轮请求再把这些工具的完整 schema 加进来

这相当于把“工具发现”从“直接可见”变成“按需加载”。

---

## 4. 哪些工具会被延迟加载

不是所有工具都适合延迟加载。  
系统通过一套明确规则判断一个工具是不是 deferred tool。

通常会被延迟加载的工具有：

- MCP 工具
- 显式声明 `shouldDefer` 的工具

但也有例外：

- 标记为 always load 的工具不会延迟加载
- ToolSearch 本身不能被延迟加载，因为它自己就是“加载其他工具”的入口
- 某些必须首轮可见的核心工具也会强制常驻

这一步的本质是一个“工具可见性策略”。

你可以把它理解成：

- 常驻工具：模型一开始就能看到完整 schema
- 延迟工具：模型先知道它存在，但不能立刻调用，必须先搜索再加载

---

## 5. ToolSearch 什么时候启用

ToolSearch 并不是永远开启的，它会综合几个条件判断：

### 1）模型是否支持相关能力

不是所有模型都支持这种工具加载机制。  
如果模型不支持相应的 tool_reference 语义，就直接关闭 ToolSearch。

### 2）当前工具池里有没有 ToolSearch

如果 ToolSearch 自己都不在允许列表里，那当然没法用。

### 3）当前模式是不是允许 ToolSearch

系统支持几种模式：

- 永久在线
- 自动模式
- 标准模式

其中自动模式会进一步判断 deferred tools 的规模是否足够大，值不值得启用 ToolSearch。

### 4）延迟工具是否真的存在

如果当前没有任何 deferred tool，而且也没有 MCP server 正在连接，那么 ToolSearch 没必要开启。

所以最终启用与否，不是单点决策，而是一个综合门控。

---

## 6. 什么时候工具会被真正“交给模型”

这是理解整套机制的关键。

在启用了 ToolSearch 的情况下，工具不会一次性全部送出，而是分成两类：

### 第一类：非 deferred 工具

这类工具始终完整暴露给模型。  
比如一些核心文件工具、搜索工具、普通操作工具等。

### 第二类：deferred 工具

这类工具只有在“已经被发现”之后才会被加入本轮请求。

所谓“已经被发现”，指的是模型历史里出现过 ToolSearch 的返回结果。  
系统会扫描历史消息，找到里面的 tool_reference，再把这些工具名当作已发现工具。

这就形成了一个闭环：

1. 模型先调用 ToolSearch
2. ToolSearch 返回某些工具名
3. 下一轮请求里，这些工具的完整 schema 被加入
4. 模型终于可以直接调用这些工具

---

## 7. ToolSearch 是怎么工作的

ToolSearch 的实现其实很实用主义，不是向量搜索，不是 embedding，也不是复杂的语义检索，而是一个规则化的匹配器。

它支持两种主要方式：

### 1）精确选择

模型可以直接输入类似“select:某个工具名”的形式。  
这会把指定工具直接拉出来。

这对模型很友好，因为当它已经知道自己想要哪个工具时，不需要再经历模糊搜索。

### 2）关键词搜索

如果模型只给出一些关键词，ToolSearch 会做一套打分逻辑：

- 工具名拆分后的词是否匹配
- 是否命中 MCP server 名称
- 是否命中工具描述
- 是否命中 search hint
- 是否包含必须词

最后按分数排序，返回前几个最相关的候选项。

### 为什么这种方案有效

因为 ToolSearch 的任务不是“理解世界”，而是“在已知工具池里快速找到候选工具”。

对于这个任务，规则化检索比复杂模型检索更稳定：

- 可解释
- 可控
- 容易调优
- 运行成本低
- 对工具命名和描述高度敏感

也就是说，它本质上是一个“面向工具目录的搜索引擎”，而不是一个通用的语义搜索系统。

---

## 8. ToolSearch 返回的不是工具本体，而是“可引用标记”

这一点很重要。

ToolSearch 结果不会直接把完整工具 schema 一股脑返回给模型，而是返回一种引用式结果。  
模型随后会在下一轮请求中，自动把这些被引用的工具视为“已发现工具”。

换句话说：

- ToolSearch 负责“发现”
- API 请求负责“加载”
- 模型负责“选择”

这三者是分离的。

这样设计的好处是：

- 发现与加载解耦
- 可以增量打开工具能力
- 不需要在初始 prompt 里塞进所有 schema

---

## 9. 模型如何真正“选择”工具

到了这一步，就进入很多人最容易误解的地方：  
模型不是靠本地函数调用表自动路由，而是根据 prompt 里可见的信息自己生成 tool_use。

它的决策依据主要是：

- 工具名
- 工具描述
- 参数 schema
- 任务上下文
- 历史对话中的工具使用痕迹

所以 Tool 选择本质上是一种“模型内的分类/生成决策”：

1. 模型看见可用工具集合
2. 根据用户意图和上下文推断要不要用 tool
3. 如果要用，输出一个 tool_use block
4. 本地执行层收到 tool_use 后再验证

因此，“选 tool”不是本地代码做主导，而是模型在受控工具空间里作出的生成式选择。

---

## 10. 模型选完之后，本地执行层会做什么

模型输出 tool_use 之后，并不意味着工具就真的执行了。  
本地执行层还会做一整套严格校验。

### 第一步：按名字找工具

系统会先在当前可见工具池里按名字找对应 tool。  
如果找不到，再检查是否存在旧名字的 alias。

这一步相当于最终路由。

### 第二步：校验输入格式

执行前会先做 schema 校验。  
如果模型生成的参数不符合 schema，就不会执行，而是把错误返回给模型，让它重试。

这一步可以防止：

- 参数类型错误
- 缺字段
- 传了多余字段
- 格式不合法

### 第三步：做工具特定的权限和安全检查

即使 schema 对了，也不代表能执行。  
不同工具还有自己的权限检查、用户确认、沙箱限制、安全策略。

比如：

- shell 命令可能要过权限规则
- 文件编辑可能要过路径安全检查
- 某些危险操作可能需要用户确认
- 某些工具在后台 agent 里会被禁止

### 第四步：真正执行并回传结果

通过所有校验后，工具才会执行。  
结果再被包装成 tool_result 回到模型上下文里，供下一轮推理使用。

---

## 11. 为什么经常会出现“先搜再用”的设计

这不是偶然，而是这个系统刻意做出来的。

原因主要有四个：

### 1）减少上下文膨胀

如果把所有 MCP 工具一次性塞进去，prompt 会迅速变大。  
ToolSearch 让大部分工具按需加载，节省上下文。

### 2）提高模型选工具的准确率

工具太多时，模型更容易误选。  
先搜索再加载，等于先把候选空间缩小。

### 3）改善缓存命中

工具 schema 是 prompt 的一部分。  
工具变化越频繁，缓存越容易失效。  
延迟加载能减少无关 schema 进入初始缓存。

### 4）支持动态工具接入

MCP server 可能是后连接的。  
ToolSearch 让“工具先上线、后可用”的动态过程成为可能。

---

## 12. 这套实现最值得注意的几个细节

### 细节一：工具可见性和工具可执行性是两回事

模型能看到某个工具，不代表它一定能执行。  
执行时还会再过一遍权限、输入校验、运行环境检查。

### 细节二：搜索结果依赖历史消息

已发现工具不是存在某个全局表里，而是从历史消息中的引用提取出来的。  
这意味着工具发现是“会话态”的，不是静态态的。

### 细节三：ToolSearch 不是纯语义搜索

它更接近一个“词法 + 词义混合评分”的检索器。  
工具名结构、搜索提示、描述文本都参与打分。

### 细节四：支持旧工具名兼容

执行层会兼容 alias，这样历史记录或旧提示里的旧名字也能继续工作。

### 细节五：延迟加载是为了系统稳定，而不是为了省事

它解决的是上下文规模、缓存、动态接入、模型误选等系统问题。  
不是单纯的工程优化，而是架构必要条件。

---

## 13. 用一句完整的话概括整个机制

这套系统的“tool 选择/搜索”流程可以概括成：

> 先由客户端根据权限、模式、模型能力和工具状态构建工具池，再把可见工具的 schema 发送给模型；对延迟工具则先通过 ToolSearch 按名字或关键词发现，待工具被引用后再在后续请求中加载完整 schema；模型最终输出 tool_use，执行层再按名字、schema 和权限进行严格校验并运行。

---

## 14. 如果你要真正掌握它，建议用这条思维链记忆

记住这个顺序就够了：

- **构建工具池**
- **判断是否启用 ToolSearch**
- **区分常驻工具与延迟工具**
- **ToolSearch 负责发现候选**
- **历史消息记录已发现工具**
- **下一轮请求加载这些工具**
- **模型输出 tool_use**
- **本地执行层做验证和执行**

只要把这八步串起来，你就能理解这套实现的全部逻辑。
